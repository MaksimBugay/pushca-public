spring:
  application:
    name: binary-proxy
  webflux:
    base-path: /

management:
  endpoint.health.show-details: always
  endpoints.web.exposure:
      include: '*'
      exclude: 'shutdown'

logging:
  level:
    root: INFO

api-gateway:
  response:
    timeout:
        ms: 5000
